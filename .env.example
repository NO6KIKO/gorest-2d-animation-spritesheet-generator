# Optional.
# Local spritesheet and scene editing does not require an API key.
# Add keys only if you enable cloud image generation features.
GEMINI_API_KEY=""
EVOLINK_API_KEY=""

# Optional local server port. Defaults to 3000.
PORT=3000
